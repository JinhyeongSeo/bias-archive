---
phase: 01-foundation
plan: 01
type: execute
depends_on: []
files_modified: [package.json, tsconfig.json, tailwind.config.ts, next.config.ts, .env.local, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css]
---

<objective>
Next.js 프로젝트 생성 및 기본 설정

Purpose: 앱의 기술 기반을 구축 - TypeScript, Tailwind CSS, App Router 설정
Output: 실행 가능한 Next.js 프로젝트, 깔끔한 폴더 구조, 개발 환경 완비
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Tech stack decided:** Next.js (App Router) + TypeScript + Tailwind CSS
**Hosting:** Vercel (free tier)
**Constraints:** 무료 서비스만 사용
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js project with TypeScript and Tailwind</name>
  <files>package.json, tsconfig.json, tailwind.config.ts, next.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, postcss.config.mjs</files>
  <action>
Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-pnpm` in the project root.

Accept defaults for all prompts. This creates:
- App Router structure in `src/app/`
- TypeScript configuration
- Tailwind CSS setup
- ESLint configuration
- pnpm as package manager

After creation, clean up the default page content:
- Replace `src/app/page.tsx` with minimal placeholder ("내 최애 아카이브" heading)
- Simplify `src/app/globals.css` to keep only Tailwind directives and basic dark mode support
- Keep the default layout.tsx but update metadata title to "내 최애 아카이브"
  </action>
  <verify>
pnpm dev runs without errors
http://localhost:3000 shows the placeholder page
pnpm build completes successfully
  </verify>
  <done>
Next.js dev server runs at localhost:3000
Build passes with no TypeScript errors
Clean project structure in place
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure project structure and environment</name>
  <files>src/lib/.gitkeep, src/components/.gitkeep, src/types/.gitkeep, .env.local, .env.example, .gitignore</files>
  <action>
Create folder structure for future development:
- `src/lib/` - utility functions, database client, etc.
- `src/components/` - reusable React components
- `src/types/` - TypeScript type definitions

Create environment file structure:
- `.env.local` with placeholder comments for Supabase vars:
  ```
  # Supabase - will be configured in Phase 01-02
  NEXT_PUBLIC_SUPABASE_URL=
  NEXT_PUBLIC_SUPABASE_ANON_KEY=
  ```
- `.env.example` with same structure (for git tracking)

Verify `.gitignore` includes:
- `.env*.local`
- `node_modules`
- `.next`
- Other standard Next.js ignores (should be auto-generated)
  </action>
  <verify>
ls src/ shows lib, components, types directories
cat .env.example shows Supabase placeholder vars
git status shows .env.example tracked, .env.local ignored
  </verify>
  <done>
Folder structure created: src/lib, src/components, src/types
Environment files in place with Supabase placeholders
.gitignore properly configured
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm dev` starts without errors
- [ ] `pnpm build` succeeds with no TypeScript errors
- [ ] Project structure matches: src/app, src/lib, src/components, src/types
- [ ] .env.example exists with Supabase placeholders
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript or ESLint errors
- Clean, minimal starting point for development
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
