---
phase: 01-foundation
plan: 03
type: execute
depends_on: ["01-02"]
files_modified: [src/app/layout.tsx, src/app/globals.css, src/components/Header.tsx, src/components/Sidebar.tsx, src/components/ThemeToggle.tsx]
---

<objective>
기본 UI 레이아웃 및 네비게이션

Purpose: 앱의 시각적 구조 구축 - 헤더, 사이드바, 반응형 레이아웃
Output: 작동하는 UI 쉘, 다크모드 지원, 네비게이션 컴포넌트
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./.claude/get-shit-done/templates/summary.md
./.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

**UI context:**
- 직캠/사진 링크 정리 앱
- 그리드/리스트 레이아웃 전환 예정 (Phase 5)
- 다국어 지원 예정 (Phase 6)
- 다크 모드 필수 (아이돌 팬덤 앱에 적합)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header and Theme Toggle components</name>
  <files>src/components/Header.tsx, src/components/ThemeToggle.tsx, src/app/globals.css</files>
  <action>
Create `src/components/ThemeToggle.tsx`:
- Use next-themes for theme management: `pnpm add next-themes`
- Simple button that toggles between light/dark
- Use sun/moon icons (can use simple emoji or lucide-react icons)
- Mount check to avoid hydration mismatch

Create `src/components/Header.tsx`:
- Fixed header at top
- App title "내 최애 아카이브" on left
- ThemeToggle on right
- Simple, clean design using Tailwind
- Consider using Korean font if available (Pretendard or system font)

Update `src/app/globals.css`:
- Ensure dark mode colors are defined
- Use CSS custom properties for theme colors
- Basic color scheme:
  - Light: white bg, gray text
  - Dark: gray-900 bg, gray-100 text
  </action>
  <verify>
pnpm build passes
ThemeToggle can be imported
Header can be imported
  </verify>
  <done>
ThemeToggle component works with next-themes
Header component displays title and theme toggle
Dark mode colors defined in globals.css
  </done>
</task>

<task type="auto">
  <name>Task 2: Create layout structure with Sidebar placeholder</name>
  <files>src/components/Sidebar.tsx, src/app/layout.tsx, src/app/page.tsx</files>
  <action>
Create `src/components/Sidebar.tsx`:
- Placeholder sidebar for future navigation
- Simple structure:
  - "최애 목록" section header (for Phase 3)
  - "태그" section header (for Phase 3)
  - Placeholder text: "아직 최애가 없습니다"
- Mobile: hidden by default, can be toggled (basic state)
- Desktop: visible on left side

Update `src/app/layout.tsx`:
- Wrap children with ThemeProvider from next-themes
- Structure: Header + (Sidebar + Main content area)
- Flexbox layout: sidebar fixed width (240px), main content flexible
- Mobile responsive: sidebar hidden, full width content

Update `src/app/page.tsx`:
- Empty state message: "링크를 추가하세요"
- Center aligned, subtle styling
- This will be replaced by actual link grid in Phase 2
  </action>
  <verify>
pnpm dev shows layout with header, sidebar, main area
Resize browser to mobile width - sidebar should hide
Theme toggle switches between light and dark mode
  </verify>
  <done>
Layout structure complete: Header, Sidebar, Main
Responsive design works (desktop/mobile)
Dark mode toggle functional
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete UI shell with Header, Sidebar, Theme toggle, and responsive layout</what-built>
  <how-to-verify>
    1. Run: pnpm dev
    2. Visit: http://localhost:3000
    3. Check desktop (>1024px):
       - Header at top with "내 최애 아카이브" title
       - Sidebar on left (~240px wide)
       - Main content area shows empty state
    4. Click theme toggle:
       - Should switch between light and dark mode
       - Colors should be pleasant and readable
    5. Resize to mobile (<768px):
       - Sidebar should be hidden
       - Content should be full width
    6. Confirm: Overall layout feels clean and usable
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds with no errors
- [ ] `pnpm dev` shows complete layout
- [ ] Theme toggle works (light/dark switch)
- [ ] Responsive design works (desktop/mobile)
- [ ] No hydration errors in console
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Human verified UI looks correct
- No console errors or warnings
- Phase 1 Foundation complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`

This completes Phase 1: Foundation. The app now has:
- Next.js project with TypeScript and Tailwind
- Supabase database with core schema
- Basic UI shell ready for feature development
</output>
