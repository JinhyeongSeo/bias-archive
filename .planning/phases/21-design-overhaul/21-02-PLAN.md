---
phase: 21-design-overhaul
plan: 02
type: execute
depends_on: ["21-01"]
files_modified: [src/components/Header.tsx, src/components/Sidebar.tsx, src/components/LinkForm.tsx]
---

<objective>
핵심 레이아웃 컴포넌트에 애니메이션 적용: Header, Sidebar, LinkForm

Purpose: 앱의 주요 프레임 컴포넌트들에 토스 스타일 마이크로인터랙션 추가
Output: 부드러운 hover/press 효과, 폼 상태 전환 애니메이션이 적용된 컴포넌트
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-design-overhaul/DISCOVERY.md
@.planning/phases/21-design-overhaul/21-01-SUMMARY.md
@src/lib/animations.ts
@src/components/Header.tsx
@src/components/Sidebar.tsx
@src/components/LinkForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Animate Header navigation and toggles</name>
  <files>src/components/Header.tsx</files>
  <action>
framer-motion의 motion 컴포넌트로 Header 내 버튼/링크에 인터랙션 추가.

1. 로고 링크: hover 시 미세한 스케일 (1.02), press 시 0.98
2. 네비게이션 링크 (GIF): hover underline 대신 부드러운 배경색 전환
3. 우측 토글 버튼들: press 효과 추가 (whileTap: { scale: 0.95 })

motion.a, motion.button 사용.
import { motion } from 'framer-motion' 추가.
animations.ts의 pressScale, transitions.quickSpring 활용.

기존 Tailwind transition-colors 유지하면서 framer-motion 효과 추가.
  </action>
  <verify>Header 버튼들 클릭 시 스케일 애니메이션 동작 확인</verify>
  <done>Header의 모든 인터랙티브 요소에 press/hover 효과 적용</done>
</task>

<task type="auto">
  <name>Task 2: Animate Sidebar buttons and tag chips</name>
  <files>src/components/Sidebar.tsx</files>
  <action>
Sidebar 내 버튼과 태그 칩에 인터랙션 효과 추가.

1. 외부 검색 버튼: press 효과 (whileTap: { scale: 0.97 })
2. 플랫폼 필터 버튼들: press 효과 + 선택 시 부드러운 배경 전환
3. 태그 버튼들: press 효과 (whileTap: { scale: 0.95 })
4. 데이터 관리 버튼: press 효과

motion.button으로 교체.
animations.ts의 transitions.quickSpring 활용.
기존 Tailwind transition-colors 유지.
  </action>
  <verify>Sidebar 버튼들 클릭 시 스케일 애니메이션 동작 확인</verify>
  <done>Sidebar의 모든 버튼/태그에 press 효과 적용</done>
</task>

<task type="auto">
  <name>Task 3: Animate LinkForm preview appearance</name>
  <files>src/components/LinkForm.tsx</files>
  <action>
LinkForm에서 미리보기 카드가 나타날 때 애니메이션 추가.

1. 미리보기 컨테이너: AnimatePresence로 감싸고 slideUp 애니메이션
2. 추가/저장 버튼: press 효과 (whileTap: { scale: 0.97 })
3. 에러 메시지: fadeIn 애니메이션으로 부드럽게 나타남
4. 로딩 상태: 버튼 내 로딩 텍스트 부드러운 전환

import { motion, AnimatePresence } from 'framer-motion' 추가.
animations.ts의 variants.slideUp, transitions.basicSpring 활용.

preview && ( ... ) 조건부 렌더링을 AnimatePresence로 감싸서 exit 애니메이션 지원.
  </action>
  <verify>URL 입력 후 미리보기 나타날 때 슬라이드 업 애니메이션 확인</verify>
  <done>LinkForm 미리보기가 부드럽게 나타나고 사라짐, 버튼 press 효과 적용</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Header 버튼 press 효과 동작
- [ ] Sidebar 태그 버튼 press 효과 동작
- [ ] LinkForm 미리보기 slideUp 애니메이션 동작
- [ ] npm run build 성공
</verification>

<success_criteria>

- Header 모든 인터랙티브 요소에 press/hover 효과
- Sidebar 버튼/태그에 press 효과
- LinkForm 미리보기 출현/퇴장 애니메이션
- 빌드 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/21-design-overhaul/21-02-SUMMARY.md`
</output>
