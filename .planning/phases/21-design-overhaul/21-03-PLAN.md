---
phase: 21-design-overhaul
plan: 03
type: execute
depends_on: ["21-02"]
files_modified: [src/components/LinkCard.tsx, src/components/LinkList.tsx]
---

<objective>
카드 및 리스트 애니메이션: LinkCard hover/press 효과, LinkList stagger 애니메이션

Purpose: 링크 목록의 시각적 피드백 강화 및 리스트 로딩 시 순차적 애니메이션 효과
Output: 카드 인터랙션 개선, 리스트 stagger 효과로 더 생동감 있는 UI
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-design-overhaul/DISCOVERY.md
@.planning/phases/21-design-overhaul/21-02-SUMMARY.md
@src/lib/animations.ts
@src/components/LinkCard.tsx
@src/components/LinkList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LinkCard hover and press effects</name>
  <files>src/components/LinkCard.tsx</files>
  <action>
LinkCard 컴포넌트에 토스 스타일 hover/press 인터랙션 추가.

1. 카드 전체: motion.div로 변경, whileHover: { y: -2, scale: 1.01 }, whileTap: { scale: 0.99 }
2. 액션 버튼들 (재생, 링크, 태그, 삭제): press 효과 (whileTap: { scale: 0.9 })
3. 썸네일 영역: hover 시 오버레이 부드러운 페이드 (기존 transition-colors → framer-motion)
4. 플레이 버튼 오버레이: hover 시 opacity 전환을 motion으로 부드럽게

motion 컴포넌트 import, animations.ts의 transitions.quickSpring 활용.
기존 group-hover:opacity 패턴은 유지하되, 추가 press 효과 적용.

주의: layout='list'와 layout='grid' 모두에 일관되게 적용.
  </action>
  <verify>카드 hover 시 미세하게 떠오름, 클릭 시 눌림 효과 확인</verify>
  <done>LinkCard에 hover lift + press down 효과 적용, 액션 버튼 press 효과</done>
</task>

<task type="auto">
  <name>Task 2: Add LinkList stagger animation</name>
  <files>src/components/LinkList.tsx</files>
  <action>
LinkList 컴포넌트에서 카드 목록이 로드될 때 stagger 애니메이션 적용.

1. 리스트 컨테이너: motion.div로 변경, variants에 staggerChildren: 0.05 설정
2. 각 LinkCard 래퍼: motion.div로 감싸고 variants.slideUp 적용
3. 초기 로드 시에만 stagger, 이후 필터 변경 시에는 빠른 전환

구현:
- 리스트 컨테이너에 initial="initial", animate="animate" 설정
- 자식 카드들은 부모의 variants를 상속받아 자동 stagger

stagger 설정 (animations.ts의 값 활용):
const listContainer = {
  animate: { transition: { staggerChildren: 0.05, delayChildren: 0.1 } }
}

const listItem = {
  initial: { opacity: 0, y: 10 },
  animate: { opacity: 1, y: 0, transition: transitions.quickSpring }
}

key prop 유지하여 React reconciliation 정상 동작하도록.
  </action>
  <verify>페이지 로드 시 카드들이 순차적으로 나타나는 stagger 효과 확인</verify>
  <done>LinkList에 stagger 애니메이션 적용, 카드들이 순차적으로 등장</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] LinkCard hover 시 y -2px 이동
- [ ] LinkCard press 시 scale 0.99 적용
- [ ] LinkList 초기 로드 시 stagger 효과 동작
- [ ] npm run build 성공
</verification>

<success_criteria>

- LinkCard에 hover lift + press down 효과
- LinkCard 액션 버튼에 press 효과
- LinkList stagger 애니메이션으로 카드 순차 등장
- 빌드 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/21-design-overhaul/21-03-SUMMARY.md`
</output>
