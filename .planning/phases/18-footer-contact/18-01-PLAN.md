---
phase: 18-footer-contact
plan: 01
type: execute
depends_on: []
files_modified: [src/components/Footer.tsx, src/app/[locale]/layout.tsx, messages/ko.json, messages/en.json]
---

<objective>
Footer 컴포넌트 추가 및 연락처 정보 표시

Purpose: 페이지 하단에 연락처 정보(이메일, GitHub, Twitter/X)를 표시하여 사용자와의 소통 채널 제공
Output: Footer.tsx 컴포넌트, 레이아웃 통합, 다국어 지원
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/Header.tsx
@src/app/[locale]/layout.tsx
@messages/ko.json

**Tech stack available:** Next.js 15, next-intl, next-themes, Tailwind CSS
**Established patterns:** Header.tsx의 fixed 헤더 스타일링, useTranslations 훅 사용, 다크모드 클래스
</context>

<tasks>

<task type="auto">
  <name>Task 1: Footer 컴포넌트 생성 및 다국어 메시지 추가</name>
  <files>src/components/Footer.tsx, messages/ko.json, messages/en.json</files>
  <action>
Footer.tsx 생성:
- 'use client' 클라이언트 컴포넌트
- useTranslations 훅으로 다국어 지원
- 반응형 레이아웃 (모바일: 세로 정렬, 데스크톱: 가로 정렬)
- 다크모드 지원 (dark: 클래스 사용)
- 연락처 아이콘: GitHub, Twitter/X, Email (SVG 인라인)
- 저작권 표시: "2026 내 최애 아카이브"
- 스타일: border-t 상단 테두리, bg-white dark:bg-zinc-950, 최소 높이 적용

messages/ko.json, messages/en.json에 footer 섹션 추가:
- footer.contact: "문의하기" / "Contact"
- footer.copyright: "2026 내 최애 아카이브. All rights reserved." / "2026 My Bias Archive. All rights reserved."
- footer.github: "GitHub" / "GitHub"
- footer.twitter: "Twitter" / "Twitter"
- footer.email: "이메일" / "Email"

연락처 링크는 실제 URL 대신 placeholder 사용 (# 또는 mailto:contact@example.com)
사용자가 나중에 실제 연락처로 변경할 수 있도록 설계
  </action>
  <verify>messages/ko.json, messages/en.json에 footer 키가 존재하고, src/components/Footer.tsx 파일이 존재함</verify>
  <done>Footer.tsx 컴포넌트 생성 완료, 다국어 메시지 추가 완료</done>
</task>

<task type="auto">
  <name>Task 2: 레이아웃에 Footer 통합</name>
  <files>src/app/[locale]/layout.tsx</files>
  <action>
layout.tsx 수정:
- Footer 컴포넌트 import 추가
- main 태그 아래에 Footer 추가
- main의 min-h 조정: min-h-[calc(100vh-3.5rem-footer높이)] 또는 flex 레이아웃으로 footer를 페이지 하단에 고정

구조:
```
<body>
  <Providers>
    <Header />
    <main className="pt-14 min-h-[calc(100vh-3.5rem)]">
      {children}
    </main>
    <Footer />
  </Providers>
</body>
```

Footer는 fixed가 아닌 flow 레이아웃 (콘텐츠 아래에 자연스럽게 위치)
콘텐츠가 짧아도 Footer가 뷰포트 하단에 위치하도록 flex-grow 사용
  </action>
  <verify>npm run build 성공, 페이지 하단에 Footer가 표시됨</verify>
  <done>Footer가 레이아웃에 통합되어 모든 페이지에 표시됨</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` 성공
- [ ] `/ko` 페이지에서 한글 Footer 표시
- [ ] `/en` 페이지에서 영어 Footer 표시
- [ ] 다크모드에서 Footer 스타일 정상
- [ ] 모바일/데스크톱 반응형 레이아웃 정상
</verification>

<success_criteria>

- Footer 컴포넌트가 모든 페이지에 표시됨
- 다국어 지원 (한국어/영어)
- 다크모드 지원
- 반응형 디자인
- Phase 18 완료
</success_criteria>

<output>
After completion, create `.planning/phases/18-footer-contact/18-01-SUMMARY.md`
</output>
