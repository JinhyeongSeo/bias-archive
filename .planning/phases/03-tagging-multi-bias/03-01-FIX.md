---
phase: 03-tagging-multi-bias
plan: 03-01-FIX
type: fix
---

<objective>
Fix 1 UAT issue from plan 03-01.

Source: 03-01-ISSUES.md
Priority: 0 critical, 0 major, 1 minor
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

**Issues being fixed:**
@.planning/phases/03-tagging-multi-bias/03-01-ISSUES.md

**Original plan for reference:**
@.planning/phases/03-tagging-multi-bias/03-01-PLAN.md

**Related files:**
@src/components/Sidebar.tsx
@src/components/BiasManager.tsx
</context>

<tasks>
<task type="auto">
  <name>Fix UAT-001: 최애 추가 후 목록 실시간 업데이트</name>
  <files>src/components/Sidebar.tsx</files>
  <action>
현재 코드에서 `handleBiasChange()`가 `internalRefresh`를 증가시키면 `useEffect`가 실행되어야 하지만, 실제로 목록이 업데이트되지 않는 문제 해결.

**수정 방안:**
1. `fetchBiases()` 호출 시 `isLoading`을 true로 설정하여 재호출 보장
2. 또는 `handleBiasChange()` 에서 직접 `fetchBiases()` 호출

**구현:**
- `handleBiasChange()` 함수에서 `setInternalRefresh` 대신 직접 `fetchBiases()` 호출하도록 수정
- 이 방식이 더 직관적이고 즉각적인 업데이트 보장

**주의사항:**
- 기존 `refreshTrigger` prop과의 호환성 유지
- `useCallback` 의존성 배열 확인
  </action>
  <verify>
1. 앱 실행 (npm run dev)
2. 사이드바에서 "+" 클릭하여 새 최애 추가
3. 추가 즉시 목록에 새 최애가 표시되는지 확인
4. 새로고침 없이 바로 보이면 성공
  </verify>
  <done>최애 추가 즉시 사이드바 목록에 반영됨</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] 최애 추가 후 즉시 목록에 표시됨
- [ ] 최애 삭제 후 즉시 목록에서 제거됨
- [ ] `npm run build` 에러 없음
- [ ] 기존 refreshTrigger prop 기능 유지
</verification>

<success_criteria>
- UAT-001 해결: 최애 추가/삭제 시 실시간 목록 업데이트
- 빌드 성공
- 재검증 준비 완료
</success_criteria>

<output>
After completion, create `.planning/phases/03-tagging-multi-bias/03-01-FIX-SUMMARY.md`
</output>
